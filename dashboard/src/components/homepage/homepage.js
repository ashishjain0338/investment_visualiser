function HomePage(){
    // Returning the HTML-DOM
    return (
        <div>
            <p>Hello World</p>
        </div>
    )
}

// Export the function so that we can import it in App.js
export { HomePage }